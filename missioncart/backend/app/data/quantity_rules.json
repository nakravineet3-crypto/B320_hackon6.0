{
  "plates": {
    "formula": "headcount * 2.0 * 1.10",
    "pack_divide": true,
    "explanation": "{units} plates — 2 per person × {headcount} guests with 10% buffer"
  },
  "cups": {
    "formula": "headcount * 2.5 * 1.10",
    "pack_divide": true,
    "explanation": "{units} cups — drinks are consumed multiple times per guest"
  },
  "napkins": {
    "formula": "headcount * 3.0 * 1.15",
    "pack_divide": true,
    "explanation": "{units} napkins — high usage item with 15% buffer"
  },
  "balloon_set": {
    "formula": "headcount * 3.0 * 1.20",
    "pack_divide": true,
    "explanation": "{units} balloons — some pop before use, 20% buffer"
  },
  "candles": {
    "formula": "1.0 * 1.0",
    "pack_divide": false,
    "explanation": "1 pack of candles"
  },
  "return_gifts": {
    "formula": "headcount * 1.0 * 1.05",
    "pack_divide": true,
    "explanation": "{units} return gifts — 1 per child with small buffer"
  },
  "diapers": {
    "formula": "headcount * 10.0 * 1.10",
    "pack_divide": true,
    "explanation": "{units} diapers — ~10 per day with buffer"
  },
  "pet_food": {
    "formula": "headcount * 1.0 * 1.10",
    "pack_divide": true,
    "explanation": "{units} pet food packs with buffer"
  },
  "water_bottle": {
    "formula": "headcount * 1.0 * 1.0",
    "pack_divide": true,
    "explanation": "{units} water bottles — 1 per person"
  },
  "cold_drinks_water": {
    "formula": "headcount * 1.0 * 1.0",
    "pack_divide": true,
    "explanation": "{units} drinks — 1 per person"
  },
  "snacks_namkeen": {
    "formula": "headcount * 1.5 * 1.10",
    "pack_divide": true,
    "explanation": "{units} snack packets — 1.5 per person with 10% buffer"
  },
  "snacks": {
    "formula": "headcount * 1.5 * 1.10",
    "pack_divide": true,
    "explanation": "{units} snack packets — 1.5 per person with 10% buffer"
  },
  "beverages_tea_coffee": {
    "formula": "headcount * 1.0 * 1.10",
    "pack_divide": true,
    "explanation": "{units} beverages — 1 per person with buffer"
  },
  "trash_bags": {
    "formula": "headcount * 0.5 * 1.20",
    "pack_divide": true,
    "explanation": "{units} trash bags — 1 per 2 guests with buffer"
  },
  "decorations": {
    "formula": "3.0 * 1.0",
    "pack_divide": false,
    "explanation": "3 packs of decorations"
  },
  "party_supplies": {
    "formula": "headcount * 1.0 * 1.10",
    "pack_divide": true,
    "explanation": "{units} party supplies per person with buffer"
  },
  "tablecloth": {
    "formula": "headcount * 0.125 * 1.0",
    "pack_divide": false,
    "explanation": "1 tablecloth per 8 guests"
  },
  "towels": {
    "formula": "headcount * 1.0 * 1.0",
    "pack_divide": true,
    "explanation": "{units} towels — 1 per person"
  },
  "led_bulb": {
    "formula": "4.0 * 1.0",
    "pack_divide": false,
    "explanation": "4 LED bulbs for basic room lighting"
  },
  "disposable_cutlery": {
    "formula": "headcount * 2.0 * 1.10",
    "pack_divide": true,
    "explanation": "{units} cutlery pieces — 2 per person with buffer"
  },
  "dairy_eggs": {
    "formula": "headcount * 1.0 * 1.10",
    "pack_divide": true,
    "explanation": "{units} dairy packs per person"
  },
  "atta_rice_dal": {
    "formula": "headcount * 1.0 * 1.10",
    "pack_divide": true,
    "explanation": "{units} staple packs for the household"
  }
}
